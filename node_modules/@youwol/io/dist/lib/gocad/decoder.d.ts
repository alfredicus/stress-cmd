import { DataFrame } from '@youwol/dataframe';
/**
 * Create a Gocad Pointset (DataFrame) from a string buffer
 * @category Decoder
 */
export declare function decodeGocadVS(buffer: string, { shared, merge, repair }?: {
    shared?: boolean;
    merge?: boolean;
    repair?: boolean;
}): DataFrame[];
/**
 * Create a Gocad Polyline (DataFrame) from a string buffer
 * @category Decoder
 */
export declare function decodeGocadPL(buffer: string, { shared, merge, repair }?: {
    shared?: boolean;
    merge?: boolean;
    repair?: boolean;
}): DataFrame[];
/**
 * Create a Gocad Surface (DataFrame) from a string buffer
 * @category Decoder
 */
export declare function decodeGocadTS(buffer: string, { shared, merge, repair }?: {
    shared?: boolean;
    merge?: boolean;
    repair?: boolean;
}): DataFrame[];
/**
 * Create a Gocad Voule (DataFrame) from a string buffer
 * @category Decoder
 */
export declare function decodeGocadSO(buffer: string, { shared, merge, repair }?: {
    shared?: boolean;
    merge?: boolean;
    repair?: boolean;
}): DataFrame[];
