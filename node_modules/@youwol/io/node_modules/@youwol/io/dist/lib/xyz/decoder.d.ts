import { DataFrame } from "@youwol/dataframe";
/**
 * Format in order to know this is a cube:
 * ```text
 * # nx: 10
 * # ny: 10
 * # nz: 10
 * # x y z a normal stress1
 * # sizes 1 3 6
 * 0 0 0  9  1 3 2  0 0 0 0 0 0
 * ...
 * ```
 * @example
 * ```ts
 * decodeXYZ(buffer)
 * decodeXYZ(buffer, {merge: false})
 * decodeXYZ(buffer, {merge: false, shared: false})
 * ```
 * @category Decoder
 */
export declare function decodeXYZ(buffer: string, { shared, merge }?: {
    shared?: boolean;
    merge?: boolean;
}): DataFrame[];
