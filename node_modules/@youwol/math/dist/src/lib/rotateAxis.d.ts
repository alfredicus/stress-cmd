import { mat } from '.';
import { vec } from './vectors';
/**
 * Rotate a vector
 * @param axis The axis of rotation
 * @param angle The angle of rotation
 * @param v The vector to rotate
 * @returns The rotated vector (new instance)
 */
export declare function rotateAxis(axis: vec.Vector3, angle: number, v: vec.Vector3): number[];
/**
 * Get a 3D rotation matrix given an axis and an angle in degrees.
 * Axis can be either a vec.Vector3 or a string ('x', 'X', 'y', 'Y', 'z' or 'Z')
 * @param axis
 * @param angleInDeg
 * @returns [[mat.Matrix3]]
 */
export declare function getRotationAxis(axis: vec.Vector3 | string, angleInDeg: number): mat.Matrix3;
