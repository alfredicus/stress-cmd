import { Serie } from '@youwol/dataframe';
/**
 * Subtract several Series to generate a new one
 * @example
 * ```ts
 * // perform: a = b - c
 * const a = sub(
 *     df.get('b'),
 *     df.get('c'),
 * )
 * ```
 * @category Dataframe
 */
export declare const sub: (s: Serie, ...others: (Serie | number)[]) => Serie<import("@youwol/dataframe").IArray>;
