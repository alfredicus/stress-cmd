import { Serie } from '@youwol/dataframe';
/**
 * If itemSize is > 1, normalize each item independently, otherwize
 * normalize the serie (since itemSize=1).
 * @category Dataframe
 */
export declare const normalize: (s: Serie) => Serie;
