import { Serie } from '@youwol/dataframe';
/**
 * Check if two series are closely equals (at epsilon)
 * @category Dataframe
 */
export declare const closeTo: (s1: Serie, s2: Serie, eps?: number) => boolean;
