import { Serie } from '@youwol/dataframe';
/**
 * @example
 * ```ts
 * // perform a = b/c/d
 * const a = div( df.get('b'), df.get('c'), df.get('d') )
 * ```
 * @category Dataframe
 */
export declare const div: (s: Serie, ...others: (Serie | number)[]) => Serie<import("@youwol/dataframe").IArray>;
