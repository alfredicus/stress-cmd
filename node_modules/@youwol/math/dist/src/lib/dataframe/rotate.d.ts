import { Serie } from '@youwol/dataframe';
import { mat } from '..';
/**
 * @see [[getRotationAxis]] to generate a rotation matrix
 * @category Dataframe
 */
export declare const rotate: (s: Serie, rot: Serie | mat.Matrix3, inverse?: boolean) => Serie;
