import { Serie } from '@youwol/dataframe';
import { max, min } from '../minMax';
import { mean } from './mean';
import { q25, q50, q75 } from './quantile';
import { std } from './std';
/**
 * @category Dataframe/stats
 */
export declare const describe: (s: Serie) => {
    count: number;
    mean: number | number[];
    std: number;
    min: number | number[];
    max: number | number[];
    q25: number;
    q50: number;
    q75: number;
};
